;; -*- mode: outline; outline-regexp: " *[-\+]"; indent-tabs-mode: nil; fill-column: 120 -*-

todo
----

- split
 - move drop arrays into each pool
 - thread in pool as well
  - one poller and one rw thread
 - iterate at end through pool then drop

- TLS
